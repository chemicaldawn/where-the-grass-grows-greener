var style = require("users/dawnschumacher/nasa-eej:ui/map-style");

function get_legend() {
  
  // Create the base for the legend.
  var legend = ui.Panel({
    style: {
        width: '30%'
    }
  });
  
  // Add the top navigation bar.
  legend = legend.add(get_navbar());
  
  return legend;
}

function get_navbar() {
  
  var navbar = ui.Panel({
    
  })
  
  navbar.add(
    ui.Button({
      label: "About",
      style: {}
    })
  )
  navbar.add(
    ui.Button({
      label: "Univariate Data",
      style: {}
    })
  ).add(
    ui.Button({
      label: "Bivariate Data",
      style: {}
    })
  )
}

exports.get_legend = get_legend;