var map = require("users/dawnschumacher/nasa-eej:ui/map");
var legend = require("users/dawnschumacher/nasa-eej:ui/legend/legend");

// init function, run on first load.
function init() {
  
  // Replaces the base map with a cleaner, custom map.
  ui.root.clear();
  
  var spacer = ui.SplitPanel(
    map.object, legend.object
  ) 
  
  ui.root.add(spacer);
  
  // Links the layer selection buttons to the map itself.
  map.init();
  // Makes sure the buttons function as intended.
  legend.init();
}

init();
legend.update();