var map-module = require("users/dawnschumacher/nasa-eej:src/ui/map");

// Add custom, manipulable Map object
function init() {
  ui.root.clear();
  var map = map-module.get_map();
  ui.root.add(map); 
}

function draw() {
  
}

init();